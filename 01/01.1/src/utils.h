#ifndef UTILS
#define UTILS

#include "random.h"

//function used to initialize the random number generator.
//uses these files:
// Primes seed.in seed.out
void InitRandomGenerator(Random& rnd);

#endif //UTILS
